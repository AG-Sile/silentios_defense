[gd_resource type="Shader" format=2]

[resource]
code = "shader_type canvas_item;

void fragment() {
	vec4 previous_color = texture(TEXTURE, UV);
	vec4 red_color =  vec4(1.0, 0.0, 0.0,0.8);
	COLOR = red_color;
}"
